import { defineConfig } from 'unocss'

export default defineConfig({
  theme: {
    colors: {
      primary: 'var(--vp-c-text-1)',
      muted: 'var(--vp-c-text-2)',
      background: 'var(--vp-c-bg)',
      secondary: 'var(--vp-c-default-soft)',
      brand: 'var(--vp-c-brand-2)',
      line: 'var(--vp-c-border)',
    },
  },
})
